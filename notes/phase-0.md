* what i built:
    config.py test_config.py .env
* concept i acctualy learnt:
    basic of pydantic and pytest. BaseSettings auto read env vars. model_config enables local .env, required fields fail loud. src/ layout means you import the package name not src.
    iru_cache caches and can bite test.
* What tripped me up: missed .gitignore, the src. import error, testing Settings() directly to dodge the cache.
* Honest notes: I have no idea about OOP for python, only wrote scripts before. i learnt and made sure understand the lines.